

let initialState = {
    sideBar: [
        {id: 1, name: 'Dasha'},
        {id: 2, name: 'Pasha'},
        {id: 3, name: 'Glasha'},
    ]};

const sidebarReducer = (state = initialState) => {
    return state;
};

export default sidebarReducer;